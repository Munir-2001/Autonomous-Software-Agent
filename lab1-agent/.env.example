# Copy this file to `.env` and fill in your values.

# Host of the Deliveroo.js server (protocol optional — defaults to http://)
HOST=http://localhost:8080

# Either provide a TOKEN (preferred) or a NAME.
# TOKEN is obtained from the web UI (click "?" in top-right) or via REST /api/tokens.
TOKEN=
# NAME=Lab1Agent
